# Fotos de perfil pendientes de moderación — NUNCA accesibles por HTTP.
# Solo el panel admin autenticado (admin/avatar.php) puede leerlas del disco.
# Copia explícita del bloqueo de api/data/.htaccess (no depender solo de la herencia).
Order deny,allow
Deny from all
