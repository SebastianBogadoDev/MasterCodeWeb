# Variables de entorno — NUNCA subir al repo
server/.env
.env
.env.local
.env.production

# Composer — vendor/ SE incluye en git para auto-deploy en shared hosting sin SSH
# composer.lock SE incluye para builds reproducibles
# Excluir docs de vendor que pueden contener keys de ejemplo
vendor/stripe/stripe-php/README.md
vendor/stripe/stripe-php/CHANGELOG.md

# Dependencias
node_modules/
server/node_modules/

# Logs
*.log
npm-debug.log*
storage/logs/*.log

# Sistema operativo
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp

# Build (si se añade en el futuro)
dist/
build/
