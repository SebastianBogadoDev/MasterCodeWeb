{
    "name": "mastercodeweb/site",
    "description": "MasterCodeWeb — backend PHP para Stripe Checkout",
    "require": {
        "php": ">=8.0",
        "stripe/stripe-php": "^13.0"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist"
    }
}
