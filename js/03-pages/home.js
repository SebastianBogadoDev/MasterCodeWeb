/* =====================================================
   HOME PAGE
===================================================== */

export function initHome() {
  // El scroll-reveal global (scroll-reveal.js) gestiona .reveal.
  // Aquí solo lógica específica del home si se necesita.
}
