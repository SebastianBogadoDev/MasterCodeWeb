/* =====================================================
   SERVICIOS PAGE
===================================================== */

export function initServicios() {
  // El scroll-reveal global gestiona los .reveal de esta página.
}
